peer, master - handle master dying
what if a child flaps - dies and comes up before we know it died?
handle failed query to master?
error needs to be special if it is the master that we killed while looking for a chunk.

periodic requery
implement some smart queries

the heirarchy, and how that works out. (integrating things into a `node`)

chunk_store
	LRU eviction chunk_store
	chunk_store to disk
	
video "entry" into the system
video "exit" from the system


--------------

streamamanger - stream id if given wrong one
timeout -- handle out of orders
?? wrong file requested on same stream

implement a client pool
cleanign streams

us  - write scripts :D :D :D 
